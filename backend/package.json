{
  "name": "koperasi-backend",
  "version": "1.0.0",
  "description": "Backend API for Koperasi Desa using Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "test": "vitest",
    "test:coverage": "vitest --coverage"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "wrangler": "^3.28.2",
    "vitest": "^1.2.2"
  }
}
